Unity Headwear assetRef (CosmeticManager Resources.Load, no .prefab suffix)
=============================================================================

assetRef           Source FBX (project path)                        Notes
----------------   ---------------------------------------------   -----
headwear_cup_lp    Assets/Resources/Cosmetics/Hats/Cup_LP.fbx      Cup mesh; anchor + auto scale
headwear_glasses   Assets/Resources/Cosmetics/Facewear/glasses.fbx   Glasses; same Headwear slot as hats

Flutter / API: category string "HEADWEAR", assetRef exactly as above.

Build prefabs in Unity (close other Unity instances using this project, or use a temp copy):
  Menu: Tools > GetGains > Build Anchored Headwear Prefabs (Cup + Glasses)

Output (both paths must contain the same .prefab name):
  Assets/FlutterEmbed/Cosmetics/CosmeticAssets/Headwear/{assetRef}.prefab
  Assets/Resources/FlutterEmbed/Cosmetics/CosmeticAssets/Headwear/{assetRef}.prefab

Hierarchy per prefab: {assetRef} root > CosmeticAnchor > MeshHolder > (FBX instance)

Optional: Tools > GetGains > Report Selected Cosmetic Bounds — select a prefab instance in Hierarchy.
